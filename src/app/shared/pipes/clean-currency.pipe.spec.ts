import { CleanCurrencyPipe } from './clean-currency.pipe';

describe('CleanCurrencyPipe', () => {
  it('create an instance', () => {
    const pipe = new CleanCurrencyPipe();
    expect(pipe).toBeTruthy();
  });
});
